import type { ApiPollClash } from "@/lib/api"
export function ClashSummary({clash}:{clash:ApiPollClash}){return <section><h2 className="text-2xl font-bold">Clash complete</h2><div className="mt-5 grid gap-3 sm:grid-cols-3"><Metric label="Prediction score" value={clash.players.map(p=>`${p.displayName}: ${p.predictionScore}`).join(" · ")}/><Metric label="Agreement count" value={`${clash.agreementCount}`}/><Metric label="Completed rounds" value={`${clash.completedRounds} / ${clash.roundCount}`}/></div><p className="mt-4 font-semibold">{clash.winnerUserId?`${clash.players.find(p=>p.userId===clash.winnerUserId)?.displayName} wins on predictions.`:"Prediction score tied."}</p>{clash.reward.capReached&&<p className="mt-2 text-sm text-muted-foreground">Daily XP cap reached. You can still play rematches.</p>}</section>};function Metric({label,value}:{label:string;value:string}){return <div className="rounded-xl border p-4"><p className="text-sm text-muted-foreground">{label}</p><p className="mt-1 font-bold">{value}</p></div>}
