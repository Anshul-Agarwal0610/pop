"use client";import { useState } from "react";import { Button } from "@/components/ui/button";import { Input } from "@/components/ui/input"
export function PackCurator({onSave}:{onSave:(items:string[])=>void}){const [items,setItems]=useState([""]);return <section><p className="text-sm">Every proposition always uses the fixed choices Up / Against.</p>{items.map((x,i)=><div className="my-2 flex gap-2" key={i}><Input aria-label={`Proposition ${i+1}`} value={x} onChange={e=>setItems(items.map((v,n)=>n===i?e.target.value:v))}/><Button variant="ghost" onClick={()=>setItems(items.filter((_,n)=>n!==i))}>Remove</Button></div>)}<Button variant="outline" onClick={()=>setItems([...items,""])}>Add proposition</Button> <Button disabled={!items.length||items.some(x=>!x.trim())} onClick={()=>onSave(items.map(x=>x.trim()))}>Save draft</Button></section>}
