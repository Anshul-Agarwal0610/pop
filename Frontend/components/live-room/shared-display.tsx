import type { DisplayRoomSnapshot } from "@/lib/api";import { AggregateResults } from "./aggregate-results";import { Scoreboard } from "./scoreboard"
export function SharedDisplay({room}:{room:DisplayRoomSnapshot}) { return <main className="min-h-screen bg-slate-950 p-10 text-white"><header className="flex justify-between text-2xl"><span>PoP Live</span><span>Join {room.code}</span></header><section className="mx-auto mt-16 max-w-4xl text-center"><p className="uppercase opacity-70">{room.status}</p><h1 className="my-8 text-5xl font-bold">{room.round?.proposition??"Waiting for the host"}</h1>{room.round&&<AggregateResults round={room.round}/>}<div className="mx-auto mt-12 max-w-md text-left"><Scoreboard participants={room.scoreboard}/></div></section></main> }
