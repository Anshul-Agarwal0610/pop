import { render,screen } from "@testing-library/react";import { describe,expect,it } from "vitest";import { SharedDisplay } from "./shared-display"
describe("SharedDisplay",()=>{it("shows aggregates without controls, tokens, or votes",()=>{render(<SharedDisplay room={{roomId:"r",code:"JOINME",status:"Active",mode:"PredictMajority",version:4,participantCount:1,scoreboard:[{id:"p",displayName:"Alex",score:2,connected:true,eligible:true}],round:{position:0,proposition:"Cats are better",status:"Revealed",submitted:1,eligible:1,up:1,against:0}}/>);expect(screen.getByText("JOINME",{exact:false})).toBeInTheDocument();expect(screen.getByText("Cats are better")).toBeInTheDocument();for(const secret of ["Pause","Advance","Remove","End room","token","prediction"])expect(screen.queryByText(secret,{exact:false})).not.toBeInTheDocument()})})
